/**
 * Retired because it deleted users, configuration, and integration credentials.
 * Use `npm run fresh-start:reset -- --dry-run` and docs/FRESH_START.md instead.
 */

console.error("This unsafe script has been retired. Use `npm run fresh-start:reset -- --dry-run` and docs/FRESH_START.md.");
process.exitCode = 1;
